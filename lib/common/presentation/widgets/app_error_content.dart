import 'package:flutter/material.dart';

class AppErrorContent extends StatelessWidget {
  const AppErrorContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
